import {makeStyles} from "@material-ui/core/styles";

export const useNoDataStyles = makeStyles({
    root: {
        textAlign: "center",
        margin: "8px"
    },
    title: {
        marginBottom: "8px"
    }
});
