import {makeStyles} from "@material-ui/core/styles";

export const usePostCardStyles = makeStyles((theme) => ({
    root: {
        width: "100px",
        height: "150px",
    },
    img: {
        borderRadius: "20px"
    }
}))