import {makeStyles} from "@material-ui/core/styles";

export const usePostCardStyles = makeStyles((theme) => ({
    root: {},
    img: {
        borderRadius: "20px",
        width: "120px",
        height: "150px",
    }
}))