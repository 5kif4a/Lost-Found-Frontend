import {makeStyles} from "@material-ui/core/styles";

export const useStoriesStyles = makeStyles((theme) => ({
    root: {
        width: "100vw",
        height: "100vh",
        backgroundColor: "#111",
        display: "flex",
        justifyContent: "center"
    }
}))