import { makeStyles } from "@material-ui/core/styles";

export const useFeedbackStyles = makeStyles((theme) => ({
  root: {
    padding: "25px",
    paddingTop: "50px"
  },
  input: {
    marginTop: "25px"
  },
  btn: {
    marginTop: "25px"
  },
  span: {
    display: "block",
    marginTop: "25px",
    textAlign: "center"
  }
}))